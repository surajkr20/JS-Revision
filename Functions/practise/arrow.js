
const multiply = (a,b) =>{
    return a*b;
}

console.log(multiply(20, 10))

const square = (a) => console.log(Math.pow(a,2)); // finding squre using inbuilt fun.

const expoSquare = (a) => console.log(a ** 2);  // using exponential fun.

const sqrt = (a) => console.log(Math.sqrt(a)) // reverse square fun

square(23);
expoSquare(12);
sqrt(16);

